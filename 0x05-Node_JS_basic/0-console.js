// 0-console.js

/**
 * Prints a message to the console.
 * @param {string} message - The message to print.
 */
function displayMessage(message) {
    console.log(message);
}

// Exporting the function using the required format
module.exports = displayMessage;
